apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cityapi.fullname" . }}-test-connection"
  labels:
    {{- include "cityapi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cityapi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
