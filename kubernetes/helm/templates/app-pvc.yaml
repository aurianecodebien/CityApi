apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: city-api-pvc
  labels:
    app: city-api
    environment: {{ .Values.app.env_name }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.db.storage }}
